[pytest]
addopts = -k "not gordon and not grt" --ignore=tests/test_generate_pdf_report.py
testpaths = tests
norecursedirs = tests/legacy
pythonpath = .
markers =
    core: Stable core regression suite for shell, imports, ballistics, research, and analysis flows.
