[pytest]
testpaths = tests
# Exclude common large folders and legacy/integration tests that are not
# safe to collect in CI by default (they may print emojis or create GUI
# widgets at import time).
norecursedirs = .git node_modules build dist .venv tests/legacy
