name: Imports and Tests (Deprecated)

"on":
  workflow_dispatch: {}

jobs:
  deprecated:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Deprecated: use ci-tests.yml"